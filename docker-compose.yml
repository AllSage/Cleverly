name: cleverly

services:
  cleverly:
    container_name: ${CLEVERLY_CONTAINER_NAME:-cleverly}
    image: ${CLEVERLY_IMAGE:-cleverly:local}
    build:
      context: .
      args:
        PUID: ${PUID:-1000}
        PGID: ${PGID:-1000}
    user: "${PUID:-1000}:${PGID:-1000}"
    init: true
    read_only: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    tmpfs:
      - /tmp:size=${CLEVERLY_TMPFS_SIZE:-1g},mode=1777
      - /run:size=64m,mode=755
      - /var/tmp:size=256m,mode=1777
    pids_limit: ${CLEVERLY_PIDS_LIMIT:-4096}
    networks:
      - offline_private
    volumes:
      - cleverly-data:/app/data
      - cleverly-logs:/app/logs
      # Cookbook remote-server SSH identity. Cleverly can generate a key here;
      # add the shown public key to each remote server's authorized_keys.
      - cleverly-ssh:/app/.ssh
      # General runtime cache for fastembed, browser/MCP helpers, and packages.
      - cleverly-cache:/app/.cache
      # Cookbook local model cache. Inside Docker, "Local" means the Cleverly
      # container, so persist its HuggingFace cache in a Docker volume.
      - cleverly-huggingface:/app/.cache/huggingface
      # Cookbook-installed Python CLIs/packages (vLLM, llama-cpp-python, etc.)
      # land under /app/.local for the cleverly user.
      - cleverly-local:/app/.local
      # npm/npx cache for optional MCP helpers without writing to the image.
      - cleverly-npm-cache:/app/.npm
    env_file:
      - path: .env
        required: false
    environment:
      CLEVERLY_OFFLINE: "1"
      CODE_WORKSPACE_RUNNER: ${CODE_WORKSPACE_RUNNER:-worker}
      CODE_WORKSPACE_WORKER_DIR: /app/data/code-workspaces/.worker
      # Default offline mode avoids embedding model downloads on first boot.
      # Set to "1" only after pre-seeding FASTEMBED_CACHE_PATH with the model.
      CLEVERLY_OFFLINE_EMBEDDINGS: "0"
      # No-download lexical embedding fallback keeps RAG available while sealed.
      CLEVERLY_HASH_EMBEDDINGS: "1"
      SEARXNG_INSTANCE: ""
      OPENAI_API_KEY: ""
      RESEARCH_LLM_ENDPOINT: ""
      OLLAMA_BASE_URL: ""
      LLM_HOSTS: ""
      CHROMADB_HOST: chromadb
      CHROMADB_PORT: "8000"
      DATA_DIR: /app/data
      XDG_CACHE_HOME: /app/data/cache
      HF_HOME: /app/.cache/huggingface
      FASTEMBED_CACHE_PATH: /app/data/cache/fastembed
      npm_config_cache: /app/.npm
      PYTHONDONTWRITEBYTECODE: "1"
      # PUID / PGID are the user/group the container runs as. 1000 is the
      # default first user on most Linux installs. Override in .env if needed:
      #   PUID=1001
      #   PGID=1001
      PUID: ${PUID:-1000}
      PGID: ${PGID:-1000}
    depends_on:
      searxng:
        condition: service_healthy
        required: false
      chromadb:
        condition: service_started
        required: false
      cleverly_code_worker:
        condition: service_started
    restart: unless-stopped

  cleverly_code_worker:
    container_name: ${CLEVERLY_CODE_WORKER_CONTAINER_NAME:-cleverly-code-worker}
    image: ${CLEVERLY_IMAGE:-cleverly:local}
    user: "${PUID:-1000}:${PGID:-1000}"
    init: true
    read_only: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    network_mode: "none"
    tmpfs:
      - /tmp:size=512m,mode=1777
      - /run:size=32m,mode=755
      - /var/tmp:size=128m,mode=1777
    pids_limit: ${CLEVERLY_CODE_WORKER_PIDS_LIMIT:-1024}
    entrypoint: ["python", "-m", "src.code_workspace_worker"]
    volumes:
      - cleverly-data:/app/data
      - cleverly-cache:/app/.cache
      - cleverly-local:/app/.local
      - cleverly-npm-cache:/app/.npm
    environment:
      CLEVERLY_OFFLINE: "1"
      DATA_DIR: /app/data
      XDG_CACHE_HOME: /app/data/cache
      HF_HOME: /app/.cache/huggingface
      FASTEMBED_CACHE_PATH: /app/data/cache/fastembed
      npm_config_cache: /app/.npm
      PYTHONDONTWRITEBYTECODE: "1"
      CODE_WORKSPACE_WORKER_DIR: /app/data/code-workspaces/.worker
      PUID: ${PUID:-1000}
      PGID: ${PGID:-1000}
    restart: unless-stopped

  cleverly_proxy:
    container_name: ${CLEVERLY_PROXY_CONTAINER_NAME:-cleverly-proxy}
    image: ${CLEVERLY_IMAGE:-cleverly:local}
    user: "${PUID:-1000}:${PGID:-1000}"
    init: true
    read_only: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    tmpfs:
      - /tmp:size=64m,mode=1777
    entrypoint: ["python", "/app/docker/tcp_proxy.py"]
    command: ["0.0.0.0", "7000", "cleverly", "7000"]
    ports:
      - "${APP_BIND:-127.0.0.1}:${APP_PORT:-7000}:7000"
    networks:
      - offline_private
      - offline_edge
    depends_on:
      - cleverly
    restart: unless-stopped

  chromadb:
    image: ${CHROMADB_IMAGE:-ghcr.io/chroma-core/chroma:latest}
    pull_policy: never
    profiles:
      - support
    init: true
    security_opt:
      - no-new-privileges:true
    networks:
      - offline_private
    volumes:
      - chromadb-data:/data
    environment:
      - ANONYMIZED_TELEMETRY=FALSE
    restart: unless-stopped

  searxng:
    image: ${SEARXNG_IMAGE:-ghcr.io/searxng/searxng:latest}
    pull_policy: never
    profiles:
      - support
    init: true
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - DAC_OVERRIDE
      - FOWNER
    security_opt:
      - no-new-privileges:true
    tmpfs:
      - /tmp:size=128m,mode=1777
    networks:
      - offline_private
    entrypoint:
      - /bin/sh
      - -c
      - |
        set -eu
        if [ ! -s /etc/searxng/settings.yml ] || grep -q 'cleverly-local-searxng-json-2026-05-30\|__SEARXNG_SECRET__' /etc/searxng/settings.yml; then
          secret="$${SEARXNG_SECRET:-}"
          if [ -z "$$secret" ]; then
            secret="$$(python -c 'import secrets; print(secrets.token_urlsafe(48))')"
          fi
          sed "s|__SEARXNG_SECRET__|$$secret|g" /tmp/searxng-settings.yml.template > /etc/searxng/settings.yml
        fi
        exec /usr/local/searxng/entrypoint.sh
    volumes:
      - searxng-data:/etc/searxng
      - searxng-cache:/var/cache/searxng
      - ./config/searxng/settings.yml:/tmp/searxng-settings.yml.template:ro
    environment:
      - SEARXNG_BASE_URL=http://localhost:8080/
      - SEARXNG_SECRET=${SEARXNG_SECRET:-}
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8080/', timeout=5).read(1)\""]
      interval: 5s
      timeout: 6s
      retries: 20
      start_period: 10s
    restart: unless-stopped

  ntfy:
    image: docker.io/binwiederhier/ntfy
    pull_policy: never
    profiles:
      - support
    init: true
    security_opt:
      - no-new-privileges:true
    command: serve
    networks:
      - offline_private
    volumes:
      - ntfy-cache:/var/cache/ntfy
    environment:
      - NTFY_BASE_URL=${NTFY_BASE_URL:-http://localhost:8091}
    restart: unless-stopped

networks:
  offline_private:
    internal: true
  offline_edge:
    driver: bridge

volumes:
  cleverly-data:
    name: cleverly-data
  cleverly-logs:
    name: cleverly-logs
  cleverly-ssh:
    name: cleverly-ssh
  cleverly-cache:
    name: cleverly-cache
  cleverly-huggingface:
    name: cleverly-huggingface
  cleverly-local:
    name: cleverly-local
  cleverly-npm-cache:
    name: cleverly-npm-cache
  searxng-data:
    name: cleverly-searxng-data
  searxng-cache:
    name: cleverly-searxng-cache
  chromadb-data:
    name: cleverly-chromadb-data
  ntfy-cache:
    name: cleverly-ntfy-cache
